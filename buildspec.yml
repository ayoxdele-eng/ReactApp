version: 0.2

phases:
  install:
    
    commands:
      - npm i npm@latest -g
      - npm cache clean --force
      - rm -rf node_modules package-lock.json
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  files:
    - '**/*'
  
